\cvsection{SKILLS}

%{# items }%
    \cvskill{
        %{ name }%
    }{
        %{ years }%+ yrs
    }{
        %{# count }%
            %{ years }% / %{# max }% %{ items }%, years %{/ max }%
        %{/ count }%
    } \\ [-5pt]
%{/ items }%
