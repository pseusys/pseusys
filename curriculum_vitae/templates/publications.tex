\cvsection{PUBLICATIONS}

%{# items.0 }%
    \cvtext{\textcolor{maincol}{\textbf{Papers}}}
    \vspace{-4pt}
%{/ items.0 }%

%{# items }%
    \cvevent{
            %{ year }%
        }{
            %{# link }%\href{%{& link }%}{%{& title }%}%{/ link }%%{^ link }%%{& title }%%{/ link }%
        }{
            \textit{%{# authors }%%{ . }%%{/ authors }%}%{# venue }% $\cdot$ %{& venue }%%{/ venue }% $\cdot$ \textit{%{ status }%}
        }{
            %{& abstract }%
        }
    \vspace{4pt}
%{/ items }%

%{# reports.0 }%
    \cvtext{\textcolor{maincol}{\textbf{Technical Reports}}}
    \vspace{-4pt}
%{/ reports.0 }%

%{# reports }%
    \cvevent{
            %{ year }%
        }{
            %{# link }%\href{%{& link }%}{%{& title }%}%{/ link }%%{^ link }%%{& title }%%{/ link }%
        }{
            \textit{%{# authors }%%{ . }%%{/ authors }%}%{# supervisor }% $\cdot$ supervised by %{& supervisor }%%{/ supervisor }%%{# institution }%, %{& institution }%%{/ institution }% $\cdot$ \textit{%{ status }%}
        }{
            %{& abstract }%
        }
    \vspace{4pt}
%{/ reports }%

%{# theses.0 }%
    \cvtext{\textcolor{maincol}{\textbf{Theses}}}
    \vspace{-4pt}
%{/ theses.0 }%

%{# theses }%
    \cvevent{
            %{ year }%
        }{
            %{# link }%\href{%{& link }%}{%{& title }%}%{/ link }%%{^ link }%%{& title }%%{/ link }%
        }{
            %{& degree }% $\cdot$ %{& university }%%{# department }%, %{& department }%%{/ department }%%{# supervisor }% $\cdot$ supervised by %{& supervisor }%%{/ supervisor }%%{# company }%, %{& company }%%{/ company }%
        }{
            %{& abstract }%
        }
    \vspace{4pt}
%{/ theses }%
