%{# items.0 }%
    \cvsection{TEACHING}
%{/ items.0 }%

%{# items }%
    \cvevent{
            \textbf{%{ start }% -- %{ end }%}
        }{
            %{ role }%
        }{
            %{ place }%
        }{
            %{& description }%
        }
    \vspace{4pt}
%{/ items }%
