\parbox{\mpwidth}{
    %{# items.0 }%
        \cvsection{CERTIFICATIONS}
    %{/ items.0 }%

    %{# items }%
        \cvmetaevent {
            %{ name }%
        }{
            %{ result }%
        }{}{
            %{ desc }%
        }
    %{/ items }%
}
