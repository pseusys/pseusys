\cvsection{EDUCATION}

%{# items }%
    \cvevent{
            \textbf{
                %{ start }% - %{ end }%
            }
        }{
            %{ title }%
        }{
            %{ place }%
        }
        %{# result }%
        {
            %{ . }%
        }
        %{/ result }%
        %{^ result }%
        {}
        %{/ result }%
    \vfill\null
%{/ items }%
